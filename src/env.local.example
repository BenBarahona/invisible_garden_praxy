# ===========================================
# Vercel KV (Redis) Configuration
# ===========================================
# These variables are automatically provided by Vercel when you add KV storage
# For local development, you can either:
# 1. Leave these empty to use file-based storage (automatic fallback)
# 2. Or connect to your Vercel KV instance for testing

# Vercel KV connection strings (automatically set by Vercel in production)
# Get these from: https://vercel.com/dashboard → Storage → KV → .env.local tab
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# ===========================================
# Development Settings
# ===========================================
# Set to "true" to skip expensive cryptographic verification during development
SKIP_CRYPTO_VERIFICATION=true

# Node environment (automatically set by Vercel)
NODE_ENV=development

