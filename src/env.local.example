# ===========================================
# Redis/KV Storage Configuration
# ===========================================
# The app supports THREE storage backends (in order of priority):
# 1. Vercel KV (REST API) - if KV_REST_API_URL is set
# 2. Standard Redis - if REDIS_URL is set
# 3. File storage (fallback) - if neither is set

# Option 1: Vercel KV (automatically set by Vercel when you add KV storage)
# Get these from: https://vercel.com/dashboard → Storage → KV → .env.local tab
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Option 2: Standard Redis (Redis Cloud, ElastiCache, etc.)
# Format: redis://[username]:[password]@[host]:[port]
# Example: redis://default:password123@redis-12345.c256.us-east-1-2.ec2.cloud.redislabs.com:16821
REDIS_URL=

# ===========================================
# Development Settings
# ===========================================
# Set to "true" to skip expensive cryptographic verification during development
SKIP_CRYPTO_VERIFICATION=true

# Node environment (automatically set by Vercel)
NODE_ENV=development

